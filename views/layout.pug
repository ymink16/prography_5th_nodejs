doctype html
html
  head
    meta(charset="utf-8")
    meta(http-equiv="X-UA-Compatible" content="IE=edge")
    meta(name="viewport" content="width=device-width, initial-scale=1")
    meta(name="description" content="Todo App 입니다.")
    meta(name="author" content="ymink16")
    title= title
    link(href="/bootstrap/css/bootstrap.min.css" rel="stylesheet")
    link(href="/stylesheets/jumbotron-narrow.css" rel="stylesheet")
  body
    div(class="container")
      div(class="header")
        if user
          nav
            ul(class="nav nav-pills pull-right")
              li(role="presentation")
                a(href="/auth/logout") 로그아웃
        else
          nav
            ul(class="nav nav-pills pull-right")
              li(role="presentation")
                a(href="/") Home
              li(role="presentation")
                a(href="/register") 회원가입
              li(role="presentation")
                a(href="/login") 로그인
        h3(class="text-muted") Todo App 
      block content
    script(src="https://ajax.googleapis.com/ajax/libs/jquery/1.11.2/jquery.min.js")
    script(src="/bootstrap/js/bootstrap.min.js")
